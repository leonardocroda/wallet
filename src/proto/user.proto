syntax = "proto3";

package user;

service UserService {
  rpc Login (LoginDto) returns (Token) {}
}

message Token {
  string access_token=1;
}

message LoginDto {
  string password=1;
  string email=2;
}
